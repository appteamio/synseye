<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:large), :itemprop => "image", :class => "thumbnail" %>
<% else %>
  <%= large_image(@product, :itemprop => "image", :class => "thumbnail") %>
<% end %>
